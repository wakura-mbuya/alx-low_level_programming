/**
 * _strspn - determine the lenghth of a substring in a string
 * @s: pointer to the string to be examined
 * @accept: the substring being tested
 * Return: lenghth of the substring
 */
unsigned int _strspn(char *s, char *accept)
{
	/* implementation */
}
