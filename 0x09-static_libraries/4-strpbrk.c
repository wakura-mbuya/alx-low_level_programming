/**
 * _strpbrk - locate the first occurance of a character in a string
 * @s: the string being tested
 * @accept: pointer to a string containing the set of characters to be searched
 * Return: pointer to the occurance of accept
 */
char *_strpbrk(char *s, char *accept)
{
	/* implementation */
}
